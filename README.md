## Before you start
install composer : 
```
composer install
```

install npm : 
```
npm install
```

run npm : 
```
npm run dev
```


# Hope Services
## Description
lorem ipsum

## Stack
- Laravel 9.5
- Vue 2.6
- Bootstrap 4.6
- Postgresql 15.3
- Docker 20.10
- Docker-compose 1.29
- PHP 8.0


## Installation
- Clone the repository
- Run `composer install`
- Run `npm install`
- Run `npm run dev`
- Run `docker-compose up -d`
- Run `php artisan migrate`

## Usage
- Run `docker-compose up -d`
- Run `php artisan serve`
- Go to `http://localhost:8000`
- Run `php artisan migrate` to migrate the database
- Run `php artisan db:seed` to seed the database
- Run `php artisan test` to run the tests
- Run `php artisan migrate:fresh --seed` to refresh the database and seed it
- Run `php artisan migrate:fresh --seed --env=testing` to refresh the database and seed it with testing data
- Run `php artisan migrate:fresh --seed --env=production` to refresh the database and seed it with production data
- Run `php artisan migrate:fresh --seed --env=staging` to refresh the database and seed it with staging data
- Run `php artisan migrate:fresh --seed --env=development` to refresh the database and seed it with development data
- Run `php artisan migrate:fresh --seed --env=local` to refresh the database and seed it with local data
- Run `php artisan migrate:fresh --seed --env=testing --force` to refresh the database and seed it with testing data and force it
- Run `php artisan migrate:fresh --seed --env=production --force` to refresh the database and seed it with production data and force it
- Run `php artisan migrate:fresh --seed --env=staging --force` to refresh the database and seed it with staging data and force it
- Run `php artisan migrate:fresh --seed --env=development --force` to refresh the database and seed it with development data and force it
- Run `php artisan migrate:fresh --seed --env=local --force` to refresh the database and seed it with local data and force it
- Run `php artisan migrate:fresh --seed --env=testing --force --no-interaction` to refresh the database and seed it with testing data and force it and no interaction
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction` to refresh the database and seed it with production data and force it and no interaction
- Run `php artisan migrate:fresh --seed --env=staging --force --no-interaction` to refresh the database and seed it with staging data and force it and no interaction
- Run `php artisan migrate:fresh --seed --env=development --force --no-interaction` to refresh the database and seed it with development data and force it and no interaction
- Run `php artisan migrate:fresh --seed --env=local --force --no-interaction` to refresh the database and seed it with local data and force it and no interaction
- Run `php artisan migrate:fresh --seed --env=testing --force --no-interaction --step` to refresh the database and seed it with testing data and force it and no interaction and step
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step` to refresh the database and seed it with production data and force it and no interaction and step
- Run `php artisan migrate:fresh --seed --env=staging --force --no-interaction --step` to refresh the database and seed it with staging data and force it and no interaction and step
- Run `php artisan migrate:fresh --seed --env=development --force --no-interaction --step` to refresh the database and seed it with development data and force it and no interaction and step
- Run `php artisan migrate:fresh --seed --env=local --force --no-interaction --step` to refresh the database and seed it with local data and force it and no interaction and step
- Run `php artisan migrate:fresh --seed --env=testing --force --no-interaction --step --seeders=UsersTableSeeder` to refresh the database and seed it with testing data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=UsersTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=staging --force --no-interaction --step --seeders=UsersTableSeeder` to refresh the database and seed it with staging data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=development --force --no-interaction --step --seeders=UsersTableSeeder` to refresh the database and seed it with development data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=local --force --no-interaction --step --seeders=UsersTableSeeder` to refresh the database and seed it with local data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=testing --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder` to refresh the database and seed it with testing data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=staging --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder` to refresh the database and seed it with staging data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=development --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder` to refresh the database and seed it with development data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=local --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder` to refresh the database and seed it with local data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=testing --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder` to refresh the database and seed it with testing data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=staging --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder` to refresh the database and seed it with staging data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=development --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder` to refresh the database and seed it with development data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=local --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder` to refresh the database and seed it with local data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=testing --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder` to refresh the database and seed it with testing data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=staging --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder` to refresh the database and seed it with staging data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=development --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder` to refresh the database and seed it with development data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=local --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder` to refresh the database and seed it with local data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=testing --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder` to refresh the database and seed it with testing data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=staging --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder` to refresh the database and seed it with staging data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=development --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder` to refresh the database and seed it with development data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=local --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder` to refresh the database and seed it with local data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=testing --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder,PostCommentTableSeeder` to refresh the database and seed it with testing data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder,PostCommentTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=staging --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder,PostCommentTableSeeder` to refresh the database and seed it with staging data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=development --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder,PostCommentTableSeeder` to refresh the database and seed it with development data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=local --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder,PostCommentTableSeeder` to refresh the database and seed it with local data and force it and no interaction and step and seeders

## Testing
- Run `php artisan test` to run the tests
- Run `php artisan test --env=testing` to run the tests with testing data
- Run `php artisan test --env=production` to run the tests with production data
- Run `php artisan test --env=staging` to run the tests with staging data
- Run `php artisan test --env=development` to run the tests with development data
- Run `php artisan test --env=local` to run the tests with local data

## Deployment
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=UsersTableSeeder,PostsTableSeeder,CommentsTableSeeder,TagsTableSeeder,PostTagTableSeeder,PostCommentTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=UsersTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=PostsTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=CommentsTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=TagsTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=PostTagTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate:fresh --seed --env=production --force --no-interaction --step --seeders=PostCommentTableSeeder` to refresh the database and seed it with production data and force it and no interaction and step and seeders
- Run `php artisan migrate --env=production` to migrate the database

## License
MIT License

## Author
- [Tkekraken9]()
- [Estcepoire]()
- [Ny-aina9]()
